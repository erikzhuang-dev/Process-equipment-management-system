# 参考与预览记录

## 参考项目结论

公开参考仓库采用 Next.js、Tailwind 与 shadcn/ui，其设计说明定义了浅薄荷绿背景、白色大圆角卡片、深绿色主操作、灰绿色辅助文字与低对比柔和阴影。当前系统迁移了该视觉基线，但基于本项目现有 React、Express、tRPC 与 Drizzle 架构重建为设备全生命周期管理系统，而不是直接复制不同运行时的源代码。

## 预览验证结论

未登录状态已正常呈现生产工艺设备管理信息系统登录入口，包含系统名、访问说明和登录按钮。登录后将进入受保护的设备运营工作区；台账、工单、备件、统计、Excel 与权限模块均由后端角色控制。

## 已登录核验基线

管理员账户已成功进入系统。仪表盘显示设备总数、在线率、故障率、保养完成率、维修趋势、设备状态分布、库存风险和近期保养工单的真实空白态；左侧导航提供设备台账、保养计划与工单、故障与维修、备件／耗材及用户权限与日志入口。

设备台账的管理员新增表单已验证展示编号、名称、型号、规格、所属工序、位置和状态全部指定字段，状态默认“运行中”，并明确告知状态变更会保留独立历史记录。

真实账户核验使用明确标记的记录“QA-PEM-001／工艺设备核验机”，仅用于验证设备全生命周期流程，不作为业务示例数据。

该核验记录已填写型号“QA-MODEL-01”和规格“1200×800×1600 mm”，设备台账字段校验仍在继续。

该核验记录的所属工序为“装配工序”，位置为“A区-装配线-01”，其余字段均已满足设备台账新增要求。

提交该设备新增请求后，表单仍保持打开状态且列表未刷新，需检查写入接口的返回值或错误处理后再继续核验。

MySQL 主键解析兼容逻辑更新并通过单元测试后，管理员界面再次提交仍未关闭表单或刷新列表，说明实际返回结构仍需进一步确认。

数据库查询确认首次请求已真实持久化设备编号“QA-PEM-001”，主键为 1，状态为“running”。第二次提交失败的原因是设备编号唯一约束，而非写入失败；新增表单现已补充清晰的失败提示。

设备、保养计划、故障、维修与备件的新增流程已统一切换为 Drizzle MySQL 的 `$returningId()` 主键返回 API，避免依赖底层 mysql2 返回结构；改造后类型检查、18 项自动化测试和生产构建均通过。

真实数据库闭环核验已完成：设备 #1 形成 2 条状态变更历史；保养计划 #1 完成后形成 2 张关联工单；维修工单 #1 已闭环；核验备件最终库存为 3，并形成 2 条出入库流水；关联操作日志达到 10 条。设备、保养与维修的 Excel 中文字段解析测试亦已通过，当前共 20 项自动化测试通过。

管理员预览已完成全模块数据展示核验。设备台账显示 QA-PEM-001 的完整字段和运行中状态；保养页显示已完成工单及自动生成的下一周期待执行工单；维修页显示已完成故障维修；备件页显示库存数量 3、入库 5、领用 2 及完整流水；权限页显示管理员角色和对应操作日志。Excel 导入与导出入口已在三类记录页面展示，并由 2 项中文字段解析测试覆盖。普通用户访问管理员模块的拒绝和导航隐藏由权限单元测试与路由测试覆盖。由于真实账户浏览器扩展仍不可用，编辑、删除、三类 Excel 实际上传下载和真实账户角色切换仍需在会话恢复后进行端到端界面核验。

真实账户浏览器会话已恢复。设备台账的“历史”界面已显示“运行中 → 维修中”与“维修中 → 运行中”两条记录及对应时间，状态变更可追溯性得到端到端确认。

管理员已在真实账户界面打开 QA-PEM-001 的编辑表单，编号、名称、型号、规格、所属工序、位置和状态均被正确回显，准备提交可追溯的台账修改验证。

管理员已将 QA-PEM-001 的位置修改为“A区-装配线-02”并触发“设备台账已更新”成功提示，下一步将确认列表刷新结果。

设备列表已真实刷新并显示修改后位置“A区-装配线-02”。管理员已触发“导入 Excel”入口；当前浏览器未暴露隐藏上传控件的可交互索引，需进一步定位文件输入元素后执行上传。

页面 HTML 已确认存在仅接受 xlsx/xls 的隐藏文件输入控件，但浏览器自动化未向其分配可上传索引；直接上传调用被页面快照索引限制拒绝，需通过已登录会话恢复后的常规浏览器上传补充最终验证。

管理员浏览器会话恢复后，已在真实设备台账页面打开“新增设备”表单，准备创建独立的“新增后删除”核验记录，避免影响既有状态、保养和维修闭环数据。

浏览器页面刷新后回到管理员仪表盘，设备总数 1、在线率 100%、保养完成率 50%、已完成维修 1 项及下一周期待执行工单均正确呈现，证明真实数据库闭环数据已反映到数据仪表盘。

管理员已重新进入设备台账并稳定打开新增设备表单，开始填写独立的新增后删除核验设备，现有闭环设备 QA-PEM-001 将保持不变。

独立删除核验设备已填写编号“QA-PEM-DELETE-001”和名称“待删除核验设备”，后续将填充型号、规格、所属工序与位置后提交。

该核验设备已补充型号“QA-DELETE-MODEL”和规格“QA-DELETE-SPEC”，将继续填写所属工序与位置并提交新增。

该核验设备的所属工序已填写为“核验工序”，位置为“QA 区”，所有指定设备台账字段已完成填写，状态保留默认“运行中”。

删除确认后的浏览器刷新显示设备台账已从 2 条恢复为 1 条；数据库查询进一步确认仅保留 QA-PEM-001，证明 QA-PEM-DELETE-001 已被真实删除。设备台账的新增、编辑、删除及状态历史查看已完成真实管理员界面核验。

真实状态历史弹窗可见两条明细：`运行中 → 维修中` 与 `维修中 → 运行中`，均显示变更时间 `2026/08/17 15:59`。该界面证据确认状态变更历史查询链路正常。

状态历史弹窗的已保存 HTML（`3000-ifz1vkz9kfgd60rc07cs5-34349196.sg1.manus.computer_equipment_1786956002360.html`）已通过文本提取验证包含“运行中”“维修中”及两处“2026/08/17 15:59”，与弹窗中两条方向相反的状态变更记录一致。

保养页面在初次路由切换时短暂显示空态，重新加载后已正常显示工单 #1（已完成、QA 执行人、完成时间 2026/08/17 15:59）及工单 #2（待执行、计划时间 2026/09/16 15:59）。这与完成工单后自动生成下一周期工单的预期一致。

保养页查询已配置为路由挂载时强制刷新且不复用陈旧缓存。管理预览在该修复后首次载入 `/maintenance` 即稳定显示工单 #1（已完成）与工单 #2（待执行），未再出现错误空态。

根因是异步 `maintenance.list` 首次请求尚未返回时，页面将 `rows.data === undefined` 与空数组共同按“暂无保养计划或工单”渲染。现已显式展示加载态，并在挂载时强制重新获取工单；修复后的管理预览首屏直接展示工单 #1（已完成）和工单 #2（待执行）。

修复后以已登录浏览器直接打开 `/maintenance`，提取文本明确显示：`#2 工艺设备核验机 2026/09/16 15:59 待执行`，以及 `#1 工艺设备核验机 2026/08/18 15:59 已完成 QA 执行人 2026/08/17 15:59 已留痕`。该结果提供了首次进入页面即显示真实工单的端到端证据。

管理员预览的权限与审计页面显示“设备台账 · 删除（设备 #60001 · QA-PEM-DELETE-001）”及对应时间，证明已确认的专用核验设备删除操作已经审计留痕。维修页展示 QA 故障登记与已完成维修工单；备件页展示库存 3、入库 5、领用 2 与两条流水，均与真实数据库闭环核验一致。

已登录管理员浏览器在 `/repairs` 直接显示“QA 故障闭环核验”的低严重度故障及已完成维修工单；在 `/parts` 直接显示库存 3、安全库存 1、入库 5 与领用 2；在 `/users` 提取到完整操作审计文本，包括设备新增、编辑、删除、状态双向变更、保养计划与工单、故障登记、维修创建完成以及备件出入库。管理员角色“ezhuang／管理员”在角色选择器中可见。

在已登录管理员设备台账页面点击“导出 Excel”后，页面保持正常且导出动作被成功触发；当前台账包含 QA-PEM-001 的完整指定字段。

角色临时降级核验发现项目所有者的认证同步会自动恢复管理员角色，且原界面仍给出成功提示。服务端现已阻止所有者被降级为普通用户，避免写入与认证最终状态不一致；新增 2 项角色保护测试后，完整套件共 22 项测试通过。

在已登录管理员界面重新选择“普通用户”后，页面展示红色错误提示“项目所有者必须保留管理员角色”，角色选择器仍保持“管理员”。该结果确认所有者降级被阻止，前端已提供明确的可见错误反馈。

权限与审计页面现常驻显示“项目所有者必须保留管理员角色，不能降级为普通用户。”提示；已登录浏览器文本与页面截图均确认该提示在用户角色区域上方可见。

设备详情页 `/equipment/1` 已在真实管理员浏览器核验。页面完整呈现名称、供应商、每小时产能、OEE、OEE偏低原因（低于 90% 时显示）、能耗、数量、单价、折旧年数、损耗系数、合计金额、计入投资及备注；对于尚未录入的真实运营与投资数据统一显示“未录入”。合计金额按“数量 × 单价”计算，计入投资金额按开关状态决定是否计入。

开发预览复核设备台账页：侧边栏底部可见带语言图标的“English”切换按钮；设备台账列表已提供“详情”操作入口。详情字段支持在管理员新增和编辑表单中录入，并同步覆盖服务端校验、持久化与 Excel 导入导出。

发布后复核发现，生产域名当前用户会话的侧边栏未呈现中英文切换按钮，虽然开发代码中已实现该按钮。该差异需要作为发布版本一致性问题修复后重新核验；设备台账 Excel 导入导出功能则继续按清单规则复核。

管理员提交后收到“设备已新增”提示，设备台账从 1 条刷新为 2 条，并显示 QA-PEM-DELETE-001 的所有填写字段。该记录将用于验证管理员删除操作。

准备上传“设备台账_导入核验.xlsx”执行真实 Excel 导入时，个人浏览器扩展再次发生超时，当前中断源于浏览器会话稳定性而非应用接口；核验文件已成功生成于下载目录。
