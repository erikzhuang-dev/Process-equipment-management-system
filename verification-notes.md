# 系统核验记录

## 2026-08-18：全模块与数据导入复核

- 管理员会话已由应用接口确认，当前用户角色为 `admin`。
- 十台带有 `PEM-VAL-` 编码的系统验证设备已通过设备批量导入服务写入，导入日志记录为“设备台账 / 批量导入 / 10”。
- 导入前设备台账基准和十台设备导入工作簿已生成于 `/home/ubuntu/exports/process-equipment-management/`。
- 数据库关系完整性检查显示：设备 BU、工厂、供应商关联，保养、故障维修、库存流水关联，以及库存、产能、OEE、投资数值边界均无异常记录。
- 匿名预览截图的保养、备件和权限页面显示为空态，这是受保护查询未携带管理员会话时的预期表现，不能作为管理员真实数据空缺的结论。
- 独立设备详情页的并发截图未成功，需要在服务重启及完整构建后再次单独验证。

在服务重启后，类型检查、45 项 Vitest 回归测试和生产构建均已通过。导入后重新导出的设备台账共 11 行，覆盖 26 个导入导出列；其中 10 条 `PEM-VAL-` 设备的 BU、工厂、供应商、资产类别、关键等级、责任人、启用日期、保修到期日及运营投资字段均无缺失。实际业务查询进一步确认 BU1 至 BU4 的验证设备分布为 3、2、2、3 条，仪表盘设备总数与台账总数均为 11，且 `PEM-VAL-001` 详情接口可返回新增生命周期字段。

管理员浏览器代理在三次访问生产台账时均返回扩展响应超时，因此无法追加真实浏览器点击截图。该外部会话问题已在不停止工作流的前提下，由管理员身份接口、路由权限回归、数据库关系完整性查询、实际导入服务、实际导出文件复核和生产构建共同替代验证。
